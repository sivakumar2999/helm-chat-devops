apiVersion: v1
kind: Service
metadata:
  name: {{ include "amaze-app.fullname" . }}
  labels:
    {{- include "amaze-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "amaze-app.selectorLabels" . | nindent 4 }}
