apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amaze-app.fullname" . }}-test-connection"
  labels:
    {{- include "amaze-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amaze-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
